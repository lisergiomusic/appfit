name: appfit
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2
  cupertino_icons: ^1.0.8
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  provider: ^6.1.5+1
  http: ^1.6.0
  cached_network_image: ^3.3.0
  url_launcher: ^6.3.2
  font_awesome_flutter: ^11.0.0
  intl: ^0.20.2
  shimmer: ^3.0.0 # Adicionado para placeholders de carregamento
  rxdart: ^0.28.0 # Adicionado para combinar Streams de dados
  flutter_inset_shadow: ^2.0.3
  video_player: ^2.9.2
  supabase_flutter: ^2.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  fake_cloud_firestore: ^4.1.0+1
  firebase_auth_mocks: ^0.15.1

flutter:
  uses-material-design: true
  assets:
    - assets/